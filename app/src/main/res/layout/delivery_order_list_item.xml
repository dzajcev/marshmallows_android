<?xml version="1.0" encoding="utf-8"?>
<com.google.android.material.card.MaterialCardView
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/deliveryOrderLayout"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:layout_marginHorizontal="16dp"
    android:layout_marginVertical="8dp"
    app:cardBackgroundColor="@android:color/white"
    app:cardCornerRadius="16dp"
    app:cardElevation="2dp">

    <androidx.constraintlayout.widget.ConstraintLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:padding="12dp">

        <!-- === ЗАГОЛОВОК (ID + Клиент + Удалить) === -->

        <TextView
            android:id="@+id/deliveryOrderId"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:textColor="?attr/colorPrimary"
            android:textSize="16sp"
            android:textStyle="bold"
            app:layout_constraintStart_toStartOf="parent"
            app:layout_constraintTop_toTopOf="parent"
            tools:text="#1050" />

        <TextView
            android:id="@+id/deliveryOrderClientName"
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_marginStart="8dp"
            android:layout_marginEnd="8dp"
            android:ellipsize="end"
            android:maxLines="1"
            android:textColor="@android:color/black"
            android:textSize="16sp"
            android:textStyle="bold"
            app:layout_constraintBottom_toBottomOf="@+id/deliveryOrderId"
            app:layout_constraintEnd_toStartOf="@+id/deliveryOrderItemDelete"
            app:layout_constraintStart_toEndOf="@+id/deliveryOrderId"
            app:layout_constraintTop_toTopOf="@+id/deliveryOrderId"
            tools:text="Константинопольский К." />

        <!-- Кнопка удаления (делаем ее красной или нейтральной) -->
        <ImageButton
            android:id="@+id/deliveryOrderItemDelete"
            android:layout_width="32dp"
            android:layout_height="32dp"
            android:background="?attr/selectableItemBackgroundBorderless"
            android:contentDescription="Удалить из доставки"
            android:padding="4dp"
            android:scaleType="fitCenter"
            android:src="@drawable/delete_32"
            app:tint="@color/design_default_color_error"
            app:layout_constraintEnd_toEndOf="parent"
            app:layout_constraintTop_toTopOf="parent" />

        <!-- Разделитель -->
        <View
            android:id="@+id/dividerHeader"
            android:layout_width="match_parent"
            android:layout_height="1dp"
            android:layout_marginTop="8dp"
            android:background="#F0F0F0"
            app:layout_constraintTop_toBottomOf="@+id/deliveryOrderId" />

        <!-- === ОСНОВНАЯ ИНФОРМАЦИЯ === -->

        <!-- Адрес -->
        <ImageView
            android:id="@+id/iconAddress"
            android:layout_width="16dp"
            android:layout_height="16dp"
            android:layout_marginTop="12dp"
            android:src="@drawable/ic_location_on_24"
            app:tint="@android:color/darker_gray"
            app:layout_constraintStart_toStartOf="parent"
            app:layout_constraintTop_toBottomOf="@+id/dividerHeader" />

        <TextView
            android:id="@+id/deliveryOrderAddress"
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_marginStart="8dp"
            android:textColor="@android:color/black"
            android:textSize="14sp"
            app:layout_constraintEnd_toEndOf="parent"
            app:layout_constraintStart_toEndOf="@+id/iconAddress"
            app:layout_constraintTop_toTopOf="@+id/iconAddress"
            tools:text="ул. Пушкина, д. Колотушкина, кв 15, под. 2" />

        <!-- Телефон -->
        <ImageView
            android:id="@+id/iconPhone"
            android:layout_width="16dp"
            android:layout_height="16dp"
            android:layout_marginTop="8dp"
            android:src="@drawable/ic_phone_24"
            app:tint="@android:color/darker_gray"
            app:layout_constraintStart_toStartOf="parent"
            app:layout_constraintTop_toBottomOf="@+id/deliveryOrderAddress" />
        <!-- Если нет ic_phone_24, создайте Vector Asset "call" или "phone" -->

        <TextView
            android:id="@+id/deliveryOrderPhone"
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_marginStart="8dp"
            android:textColor="@android:color/black"
            android:textSize="14sp"
            app:layout_constraintBottom_toBottomOf="@+id/iconPhone"
            app:layout_constraintEnd_toEndOf="parent"
            app:layout_constraintStart_toEndOf="@+id/iconPhone"
            app:layout_constraintTop_toTopOf="@+id/iconPhone"
            tools:text="+7 (999) 000-00-00" />

        <!-- === ПАНЕЛЬ ДЕЙСТВИЙ (Кнопки) === -->

        <LinearLayout
            android:id="@+id/actionsLayout"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_marginTop="12dp"
            android:gravity="end|center_vertical"
            android:orientation="horizontal"
            app:layout_constraintTop_toBottomOf="@+id/deliveryOrderPhone">

            <!-- Товары -->
            <com.google.android.material.button.MaterialButton
                android:id="@+id/deliveryOrderListGoods"
                style="@style/Widget.Material3.Button.IconButton"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                app:icon="@drawable/list_goods_32"
                app:iconTint="?attr/colorPrimary"
                android:tooltipText="Товары"/>

            <!-- Связь (звонок/мессенджеры) -->
            <com.google.android.material.button.MaterialButton
                android:id="@+id/deliveryOrderConnect"
                style="@style/Widget.Material3.Button.IconButton"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                app:icon="@drawable/connect_32"
                app:iconTint="?attr/colorPrimary"
                android:tooltipText="Связаться"/>

            <View
                android:layout_width="0dp"
                android:layout_height="0dp"
                android:layout_weight="1"/>

            <!-- Статус "Доставлено" (Shipped) -->
            <!-- Сделаем кнопку более заметной -->
            <com.google.android.material.button.MaterialButton
                android:id="@+id/deliveryOrderShipped"
                style="@style/Widget.Material3.Button.TonalButton"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:text="Вручить"
                app:icon="@drawable/shipped_32"
                app:iconSize="20dp"/>
        </LinearLayout>

        <View
            android:id="@+id/dividerFooter"
            android:layout_width="match_parent"
            android:layout_height="1dp"
            android:layout_marginTop="8dp"
            android:background="#F0F0F0"
            app:layout_constraintTop_toBottomOf="@+id/actionsLayout" />

        <!-- === ФИНАНСЫ === -->

        <TextView
            android:id="@+id/tx1"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_marginTop="8dp"
            android:text="Сумма:"
            android:textColor="@android:color/darker_gray"
            android:textSize="12sp"
            app:layout_constraintStart_toStartOf="parent"
            app:layout_constraintTop_toBottomOf="@+id/dividerFooter" />

        <TextView
            android:id="@+id/deliveryOrderSum"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_marginStart="4dp"
            android:textColor="@android:color/black"
            android:textSize="12sp"
            app:layout_constraintBaseline_toBaselineOf="@+id/tx1"
            app:layout_constraintStart_toEndOf="@+id/tx1"
            tools:text="5000 ₽" />

        <TextView
            android:id="@+id/tx2"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="К оплате:"
            android:textColor="@android:color/darker_gray"
            android:textSize="12sp"
            android:layout_marginEnd="6dp"
            app:layout_constraintBottom_toBottomOf="@+id/deliveryOrderToPay"
            app:layout_constraintEnd_toStartOf="@+id/deliveryOrderToPay"
            app:layout_constraintTop_toTopOf="@+id/deliveryOrderToPay" />

        <TextView
            android:id="@+id/deliveryOrderToPay"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_marginTop="8dp"
            android:textColor="@color/design_default_color_error"
            android:textSize="16sp"
            android:textStyle="bold"
            app:layout_constraintEnd_toEndOf="parent"
            app:layout_constraintTop_toBottomOf="@+id/dividerFooter"
            tools:text="1200 ₽" />
        <!-- Цвет ToPay можно менять программно на зеленый, если оплачено -->

    </androidx.constraintlayout.widget.ConstraintLayout>
</com.google.android.material.card.MaterialCardView>
